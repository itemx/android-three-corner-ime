# 我用 Claude 復刻了三角輸入法

三角輸入法是一套用數字編碼輸入中文字的方法。每個字對應一組六位數字，使用者在數字鍵盤上輸入編碼，選字後即完成輸入。這套方法由王安電腦公司的胡立人、張源渭、黃克東三人設計，編碼規則是根據字形的三個角落特徵推導出數字，所以叫三角。它的特點是重碼率低，幾乎一字一碼，熟悉之後輸入速度不慢。

我自己沒有在用三角輸入法，但長輩有在用。他們從早期就學了這套輸入方式，用了三十幾年，手指記憶已經內建在肌肉裡，換別的輸入法對他們來說是不可能的事。問題出在 Android 平台上唯一的三角輸入法 App，最後一次更新是 2015 年。隨著 Android 系統版本演進，這個 App 在近代手機上已經無法正常執行。

這件事困擾了好幾年。長輩中間有想過找人重新寫一個，但三角輸入法的使用者大概就幾百人或更少。這種規模的需求，找工程師來寫不太現實，外包也麻煩，光是說明需求、來回溝通、測試修改，花的時間和金錢都不划算。就這樣一直擱著，也就變成只能使用手寫輸入，或者慢慢的打注音。

隨著最近AI的導入，在工作之餘突然想起這件事其實很適合讓 AI 來做。三角輸入法的核心邏輯很單純：讀取字碼對照表，這部分在 github 還有人保存，讓使用者輸入數字代碼，程式查表，列出候選字，選字送出。不需自然語言處理，不需機器學習，就是查表加上一個鍵盤介面。需求明確，規格清楚，但就是沒有人願意花時間寫。這恰好是 AI 輔助開發最能發揮的地方。

用 Claude 來做這件事。整個製作過程大約兩到三個小時。我先把開源的三角編碼字碼表（3corner.cin，收錄 32,913 筆字碼對應）提供給 Claude，說明我要做一個 Android 輸入法，它就開始規劃架構、建立專案、寫程式碼。

技術上，這個 App 用 Kotlin 寫，介面用 Jetpack Compose 搭配 Material 3 設計。整個專案有 14 個 Kotlin 檔，合計一千多行程式碼。鍵盤有三種模式可以切換：中文三角輸入（數字鍵盤配標點符號）、英文 QWERTY 鍵盤、符號鍵盤。另外追加了聯想字功能，用了 jieba 的繁體中文詞庫，在選完一個字之後，候選列會自動顯示常見的下一個字，這是原本的三角輸入法沒做的事情。App 也支援深色模式，最後產出的 APK 約 10MB，不過短時間內大概不會放到 Google Play。

開發過程中遇到的問題大多是 Android IME 框架本身的坑，這些問題 基本上不用介入，現在 Claude code 解讀 crash log 之後都能定位原因並修正。功能的追加也是對話式的，我說要加標點符號，它就改鍵盤佈局；我說要參照原版 App 的配置，傳了一張截圖給它，它就照著做。

把 APK 傳到家人的手機上，安裝，啟用，切換輸入法，打了幾個字，可以用。就這樣，一個停更了快十年的輸入法，在一個下午復活了。

回顧一下，一個幾百人用的輸入法，一個下午的開發時間，技術上也沒什麼創新。但它解決了一個具體的痛點。長輩不需要知道什麼是 Kotlin 或 Android Studio，他們只需要知道在手機上能夠再次使用三角輸入法打字，而且很滿意。

AI 在這裡做的事情，就是把一件「明明很簡單但就是沒人做」的事情做完了。能夠達成這件事情，我也感到開心。